@page
@model RostrosFelices.Pages.Clientes.IndexModel
@{
}
<h1>GESTION DE CLIENTES</h1>
<p>
    <a asp-page="Create" class="btn btn-success">CREA TU USUARIO AQUI</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model=>model.Clientes[0].Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model=>model.Clientes[0].Apellido)
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Clientes)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem=>item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem=>item.Apellido)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id" class="btn btn-warning">Edit</a>
                    <a asp-page="./Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                </td>
                <td>
                    <span></span>
                </td>
            </tr>
        }
    </tbody>
</table>